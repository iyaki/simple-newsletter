// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/php
{
	"name": "simple-newsletter",
	"build": {
		"dockerfile": "../Dockerfile",
		"context": "..",
		"target": "dev-environment"
	},

	"workspaceFolder": "/app",
	"workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind",
	"overrideCommand": false,
	"forwardPorts": [8080],
	"runArgs": [
		"--env-file", "${localWorkspaceFolder}/.env",
		"--memory", "8g"
	],
	"mounts": [
		{
			"source": "${localEnv:HOME}/.opencode",
			"target": "/home/root/.local/share/opencode",
			"type": "bind"
		},
		{
			"source": "${localEnv:HOME}/.local/state/opencode",
			"target": "/home/root/.local/state/opencode",
			"type": "bind"
		},
		"source=${localWorkspaceFolder}/.php/php.ini,target=/usr/local/etc/php/php.ini,type=bind",
		"source=${localWorkspaceFolder}/.php/development.ini,target=/usr/local/etc/php/conf.d/dev.ini,type=bind",
		"source=caddy_data,target=/data,type=volume",
		"source=caddy_config,target=/config,type=volume",
		{ "source": "${localEnv:HOME}/.config/gh/", "target": "/home/root/.config/gh/", "type": "bind" },
		{
      "source": "${localEnv:HOME}/.omp",
      "target": "/root/.omp",
      "type": "bind"
    }
	],
	"containerEnv": {
		"SERVER_NAME": "http://localhost:8080"
	},

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Configure tool-specific properties.
	"customizations": {
		"vscode": {
			"extensions": [
				"bmewburn.vscode-intelephense-client",
				"neilbrayfield.php-docblocker",
				"redhat.vscode-yaml"
			]
		}
	},

	// Use 'postCreateCommand' to run commands after the container is created.
	// "postCreateCommand": "sudo chmod a+x \"$(pwd)\" && sudo rm -rf /var/www/html && sudo ln -s \"$(pwd)\" /var/www/html"

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root",
	"features": {
		"ghcr.io/devcontainers/features/git:": {
			"ppa": true,
			"version": "latest"
		},
		"ghcr.io/devcontainers-extra/features/apt-packages:": {
			"packages": "wget,procps"
		},
		"ghcr.io/devcontainers-extra/features/opencode:": {},
		"ghcr.io/devcontainers-extra/features/gh-cli": {},
		"ghcr.io/iyaki/devcontainer-features/oh-my-pi:": {},
		"ghcr.io/devcontainers/features/node:": {},
		"ghcr.io/iyaki/devcontainer-features/lefthook:": {}
	}
}
