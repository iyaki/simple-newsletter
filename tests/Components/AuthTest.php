<?php

declare(strict_types=1);



test('hash and verify return true for same key', function (): void {});

test('verify returns false for different key', function (): void {});

test('hash is deterministic', function (): void {});
